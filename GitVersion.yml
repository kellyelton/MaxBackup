# GitVersion configuration - uses tags for versioning
workflow: GitHubFlow/v1
mode: ContinuousDeployment

branches:
  main:
    regex: ^master$|^main$
    label: ''
    is-main-branch: true
  test:
    regex: ^test$
    label: preview
